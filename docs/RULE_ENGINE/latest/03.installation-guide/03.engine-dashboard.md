# Rule Engine Dashboard

The web interface allows you to visualize data and manage configuration.

## Installation
```bash
apt-get install forensic-dashboard
```

The dashboard runs on port 443 by default. Default credentials are printed at the end of the installation process.
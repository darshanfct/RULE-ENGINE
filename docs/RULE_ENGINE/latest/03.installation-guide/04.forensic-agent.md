# Forensic Agent

Deploying agents to endpoints to collect logs and monitor system integrity.

## Linux Agent
```bash
curl -so agent.deb https://packages.forensic.com/agent.deb && dpkg -i agent.deb
```

## Windows Agent
1. Download the MSI installer.
2. Run via PowerShell as Administrator.
3. Register the agent with the manager IP.